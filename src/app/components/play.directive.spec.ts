import { PlayDirective } from './play.directive';

describe('PlayDirective', () => {
  it('should create an instance', () => {
    const directive = new PlayDirective();
    expect(directive).toBeTruthy();
  });
});
